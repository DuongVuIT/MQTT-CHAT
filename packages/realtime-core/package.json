{
  "name": "@mqtt-chat/realtime-core",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo 'no unit tests yet' && exit 0",
    "lint": "echo 'lint handled at root' && exit 0"
  },
  "dependencies": {
    "@mqtt-chat/mqtt-contracts": "workspace:*",
    "mqtt": "^5.10.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
