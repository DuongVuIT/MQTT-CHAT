# Skill: debugging

reproduce -> collect evidence (logs, events, DB state) -> identify layer (client/MQTT/worker/DB/Redis) -> find root cause -> minimal correct fix -> regression test -> verify gates.
