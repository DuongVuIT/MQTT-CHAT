# Skill: bot-automation

Must know:

- event router consumes chat/v1/events/#
- command parser (/cmd args)
- trigger/condition/action pipeline
- state scoped bot/conversation/user
- scheduler for delayed jobs
- cooldowns + loop prevention (ignore BOT sender unless opted-in)
- correlation/causation trace IDs
