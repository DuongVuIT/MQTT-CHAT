# Skill: monorepo

Checklist:

- apps -> packages only.
- Public API via src/index.ts.
- No deep imports between packages.
- Shared code extracted only when truly reused.
- pnpm + turbo tasks consistent across packages.
